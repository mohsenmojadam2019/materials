@extends('layouts.store')
@section('title','آدرس‌ها | '.config('store.name'))
@section('content')
<section class="simple-page"><div class="page-hero-small"><div><span>حساب مشتری</span><h1>آدرس‌های من</h1><p>آدرس‌های ارسال سفارش را مدیریت کنید.</p></div></div><div class="addresses-grid">@foreach($addresses as $a)<article class="panel address-card"><h3>{{ $a->title }}</h3><p>{{ $a->province }}، {{ $a->city }}، {{ $a->address }}</p><small>{{ $a->recipient }} • {{ $a->phone }}</small><form method="post" action="{{ route('account.addresses.delete',$a) }}">@csrf @method('DELETE')<button class="danger-link">حذف</button></form></article>@endforeach</div><form class="panel address-form" method="post" action="{{ route('account.addresses.store') }}">@csrf<h2>افزودن آدرس</h2><div class="form-grid"><label>عنوان<input name="title" required value="آدرس اصلی"></label><label>تحویل‌گیرنده<input name="recipient" required></label><label>موبایل<input name="phone" required></label><label>استان<input name="province" required></label><label>شهر<input name="city" required></label><label>کد پستی<input name="postal_code"></label><label class="span2">آدرس<textarea name="address" required></textarea></label></div><button class="primary-product-btn">ثبت آدرس</button></form></section>
@endsection