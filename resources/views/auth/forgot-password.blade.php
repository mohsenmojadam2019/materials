<!doctype html><html lang="fa" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>بازیابی رمز | {{ config('store.name') }}</title><link rel="stylesheet" href="{{ asset('assets/css/app.css') }}"></head><body class="login-page"><div class="login-shell single-auth"><section class="login-card"><a class="login-back" href="{{ route('login') }}">← بازگشت به ورود</a><h2>بازیابی رمز عبور</h2><p>ایمیل حساب را وارد کنید تا لینک بازیابی برای شما ارسال شود.</p>@if(session('success'))<div class="login-error success">{{ session('success') }}</div>@endif @if($errors->any())<div class="login-error">{{ $errors->first() }}</div>@endif<form method="post" action="{{ route('password.email') }}">@csrf<label>ایمیل<input type="email" name="email" required></label><button>ارسال لینک بازیابی</button></form></section></div></body></html>